command center is a panel to send command to any device and ask it to do some work

the user installs the app file in both asking device and the doing device

i prefer 1 html code for all

or it will be 1 html code for asking apps and 1 server in the doing device

and we have this PC which will mediate the request

this PC is the server, which gets commands from the asking devices

and it have a contract with the doing device to send it the command with all required data to verify the doing action, verify the command is doable or decline, if the command is accepted, the doing machine will run some command on the doing machine to complete the job

if the job is done we send back a done to the asker, or error if not

we will not pay to any vendor like apple or samsung or any to regiser the apps, we will always use strategies which works without this - the installation will be easy, but we will install and bootstrap the first installations to make this very easy for our customers